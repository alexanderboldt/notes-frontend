services:
  notes-frontend:
    image: ghcr.io/alexanderboldt/notes-frontend-local:0.4.0
    container_name: notes-frontend
    ports:
      - 4100:80
    networks:
      - notes

networks:
  notes:
    name: notes
    external: true
